"""Реестр задач. Импорт модуля регистрирует задачи в `benchmark.task`.

Три задачи, у каждой внешний эталон, потому что судить результат должен код,
а не модель:

* `clickgate` — офлайн, эталон считается формулой; регрессия на `resolve.py`;
* `hn`        — быстрый общий тест, эталон из Firebase API самого Hacker News;
* `cbr`       — длинная таблица, эталон из официального XML ЦБ РФ.
"""

from benchmark.tasks import cbr, clickgate, hn  # noqa: F401
